kind: Service
apiVersion: v1

metadata:
  name: nginx-svc
  namespace: nginx-ns

spec:
  selector:
    app: nginx

  ports:
    - protocol: TCP
      port: 82 # cluster port /host port [service]
      targetPort: 80 #container port


